# This is a Databricks asset bundle definition for db_proj_assetbundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: db_proj_assetbundle
  uuid: 3e7f6543-662d-495d-a821-56cb4098201d

# artifacts:
#   python_artifact:
#     type: whl
#     build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-80408f5d-45a5.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-80408f5d-45a5.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/manoritesandeep@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/manoritesandeep@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
